dot.ui.notify("start lua script...")

local zig_boilerplate = [[
// Boilerplate generated by  Lua !
const std = @import("std");

pub fn main() !void {
    std.debug.print("Hello from dot.!\n", .{});
}
]]

dot.edit.insert(zig_boilerplate)

dot.edit.insert("\n //- List")

for i = 1, 5 do
	local line = "// -> elem n<" .. tostring(i) .. ">\n"
	dot.edit.insert(line)
end

dot.ui.notify("script finished !")